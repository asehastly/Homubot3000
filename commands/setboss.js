exports.run = (homu, message, args) => {
    const Discord = require('discord.js');
    
    const stbs = args;

    if(stbs[0] === 'yae' && stbs[1] === 'sakura') {
        var boss = 'Yae Sakura'
        var lvl = stbs[3];
    } 
    
};

exports.help = {
	name: 'setboss'
}